import { BlockPasteDirective } from './block-paste.directive';

describe('BlockPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockPasteDirective();
    expect(directive).toBeTruthy();
  });
});
